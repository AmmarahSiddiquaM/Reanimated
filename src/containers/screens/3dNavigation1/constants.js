import {dimensions} from '../../../theme';

export const SCREEN_WIDTH = dimensions.screenWidth;
export const SCREEN_OFFSET = 60;
export const CONTAINER_WIDTH = 2 * SCREEN_WIDTH;
export const PERSPECTIVE = 1200;
export const ANGLE = Math.PI / 2;
